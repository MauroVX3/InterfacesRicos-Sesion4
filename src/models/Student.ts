// Cambia la carpeta entities por models por el patrón MVVM y se actualizan las importaciones correspondientes.
export interface Student { 
    id?: string; 
    firstName: string; 
    lastName: string; 
    program: string; 
};